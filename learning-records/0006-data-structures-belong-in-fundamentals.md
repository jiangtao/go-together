# 数据结构属于 Go 基础章节

用户明确纠正：数据结构不应放在后期，也不应被做成项目功能或单独算法课；它应该放在 Go 基础章节靠前的位置。后续课程设计应把 array、slice、map、set、string/byte/rune 作为 Go 表达能力的一部分，在 HTTP、DB、gRPC 之前建立清楚。

**Implications**：

- 课程保持学习主线优先，练习切片只服务于当天概念。
- 数据结构前置到 Phase 01，作为 Day 3-4 的明确主题。
- 删除任务管理式进度材料，避免把学习计划变成任务管理表。

# 协作执行规则

1. 发现可复用的原则、约束或长期规则时，及时更新本文件；一次性实现细节不写入。
2. 需求包含互不依赖的任务时，优先并行执行；最多使用 8 个 Agent，并受运行环境实际并发槽位限制。
3. 超大任务应按清晰边界拆分为多个并行 session；最多并行 3 个 session，并明确每个 session 的输入、输出与验收条件。
4. 并行单元不得写入同一文件或共享可变状态；存在依赖或冲突风险时保持串行。
5. 汇总前必须统一执行完整验证，确保各并行结果能在同一基线上集成。
